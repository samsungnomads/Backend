spring:
  datasource:
    url: jdbc:mysql://prod-db:3306/wheretogo
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: none # 🛑 스키마 변경 없음
    show-sql: false # 🔇 SQL 로그 출력 안함 